Source: Bo Ørsted Andresen <zlin@exherbo.org>
Upstream: No.
Reason: Avoid error messages such as:
	bash: BASH_COMPLETION: readonly variable

--- bash_completion/bash_completion	2008-05-18 14:06:17.000000000 +0200
+++ bash_completion/bash_completion	2008-05-18 14:17:30.000000000 +0200
@@ -28,13 +28,13 @@
 [ -n "${BASH_COMPLETION_DEBUG:-}" ] && set -v || set +v
 
 # Alter the following to reflect the location of this file.
-#
-{
-  # These declarations must go within braces in order to be able to silence
-  # readonly variable errors.
+# Don't try to redefine readonly variables
+if [[ -z ${BASH_COMPLETION} ]]; then
   BASH_COMPLETION="${BASH_COMPLETION:-/etc/bash_completion}"
+fi
+if [[ -z ${BASH_COMPLETION_DIR} ]]; then
   BASH_COMPLETION_DIR="${BASH_COMPLETION_DIR:=/etc/bash_completion.d}"
-} 2>/dev/null || :
+fi
 readonly BASH_COMPLETION BASH_COMPLETION_DIR
 
 # Set a couple of useful vars
