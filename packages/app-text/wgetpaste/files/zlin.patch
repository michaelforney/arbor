Source: Bo Ørsted Andresen
Upstream: Yes
Reason: We want a non-public pastebin service to be the default in arbor.

--- wgetpaste	2008-01-02 17:07:09.000000000 +0100
+++ wgetpaste	2008-01-02 17:08:33.000000000 +0100
@@ -14,7 +14,7 @@
 N=$'\n'
 
 ### services
-SERVICES="ca rafb osl"
+SERVICES="ca rafb osl zlin"
 # ca
 ENGINE_ca=ca
 URL_ca="http://pastebin.ca/"
@@ -33,6 +33,11 @@
 # tinyurl
 ENGINE_tinyurl=tinyurl
 URL_tinyurl="http://tinyurl.com/ create.php"
+# zlin
+ENGINE_zlin=knopaste
+URL_zlin="http://zlin.dk/p/ ?"
+EXPIRATION_INFO_zlin="Pastes on zlin: ${URL_zlin% *} do not expire."$'\n'
+DEFAULT_LANGUAGE_zlin="text"
 
 ### engines
 # ca
@@ -46,6 +51,11 @@
 POST_ca="s=Submit+Post name description type expiry % content"
 REGEX_URL_ca='s|^.*content="[0-9]*;\(http://[^/]*/[0-9]*\)".*$|\1|p'
 REGEX_RAW_ca='s|^\(http://[^/]*/\)\([0-9]*\)$|\1raw/\2|'
+# knopaste
+LANGUAGES_knopaste="actionscript ada apache applescript asm asp autoit bash blitzbasic bnf c c_mac caddcl cadlisp cfdg cfm cpp cpp-qt csharp css d delphi diff div dos eiffel fortran freebasic gml groovy html4strict idl ini inno io java java5 javascript latex lisp lua matlab mirc mpasm mysql nsis objc ocaml ocaml-brief oobas oracle8 pascal perl php php-brief plsql python qbasic reg robots ruby sas scheme sdlbasic smalltalk smarty sql tcl text thinbasic tsql vb vbnet vhdl visualfoxpro winbatch xml"
+POST_knopaste="submit=Submit % % lang % % paste"
+REGEX_knopaste='s|$|\.txt|'
+REGEX_RAW_knopaste='s|^http://.*$|&.txt|'
 # rafb
 LANGUAGES_rafb="C C89 C99 C++ C# Java Pascal Perl PHP PL/I Python Ruby SQL VB Plain%Text"
 POST_rafb="% nick desc lang % cvt_tabs text"
@@ -513,7 +523,7 @@
 done
 [[ $SERVICESET ]] && verifyservice "$SERVICESET" && SERVICE=$(escape "$SERVICESET")
 DEFAULT_NICK=${DEFAULT_NICK:-$(whoami)} || die "whoami failed"
-DEFAULT_SERVICE=${DEFAULT_SERVICE:-rafb}
+DEFAULT_SERVICE=${DEFAULT_SERVICE:-zlin}
 DEFAULT_LANGUAGE=${DEFAULT_LANGUAGE:-Plain Text}
 DEFAULT_EXPIRATION=${DEFAULT_EXPIRATION:-1 month}
 SERVICE=${SERVICE:-${DEFAULT_SERVICE}}
