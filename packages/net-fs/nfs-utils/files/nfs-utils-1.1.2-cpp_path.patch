Upstream: Wouldn't accept this, Exherbo-specific problem.
Source: philantrop@exherbo.org
Summary: 

rpcgen as provided by our glibc doesn't find /usr/bin/cpp. It's searching it in
/lib/cpp and /usr/ccs/lib/cpp only. So we're specifying the correct path here.

diff -Naur nfs-utils-1.1.2.orig/support/export/Makefile.in nfs-utils-1.1.2/support/export/Makefile.in
--- nfs-utils-1.1.2.orig/support/export/Makefile.in	2008-03-14 18:44:40.000000000 +0100
+++ nfs-utils-1.1.2/support/export/Makefile.in	2008-05-08 13:15:16.000000000 +0200
@@ -515,15 +515,15 @@
 
 $(GENFILES_CLNT): %_clnt.c: %.x $(RPCGEN)
 	test -f $@ && rm -rf $@ || true
-	$(RPCGEN) -l -o $@ $<
+	$(RPCGEN) -Y /usr/bin -l -o $@ $<
 
 $(GENFILES_XDR): %_xdr.c: %.x $(RPCGEN)
 	test -f $@ && rm -rf $@ || true
-	$(RPCGEN) -c -o $@ $<
+	$(RPCGEN) -Y /usr/bin -c -o $@ $<
 
 $(GENFILES_H): %.h: %.x $(RPCGEN)
 	test -f $@ && rm -rf $@ || true
-	$(RPCGEN) -h -o $@ $<
+	$(RPCGEN) -Y /usr/bin -h -o $@ $<
 	rm -f $(top_builddir)/support/include/mount.h
 	$(LN_S) ../export/mount.h $(top_builddir)/support/include/mount.h
 # Tell versions [3.59,3.63) of GNU make to not export all variables.
diff -Naur nfs-utils-1.1.2.orig/utils/statd/Makefile.in nfs-utils-1.1.2/utils/statd/Makefile.in
--- nfs-utils-1.1.2.orig/utils/statd/Makefile.in	2008-03-14 18:44:42.000000000 +0100
+++ nfs-utils-1.1.2/utils/statd/Makefile.in	2008-05-08 13:20:08.000000000 +0200
@@ -640,19 +640,19 @@
 
 $(GENFILES_CLNT): %_clnt.c: %.x $(RPCGEN)
 	test -f $@ && rm -rf $@ || true
-	$(RPCGEN) -l -o $@ $<
+	$(RPCGEN) -Y /usr/bin -l -o $@ $<
 
 $(GENFILES_SVC): %_svc.c: %.x $(RPCGEN)
 	test -f $@ && rm -rf $@ || true
-	$(RPCGEN) -m -o $@ $<
+	$(RPCGEN) -Y /usr/bin -m -o $@ $<
 
 $(GENFILES_XDR): %_xdr.c: %.x $(RPCGEN)
 	test -f $@ && rm -rf $@ || true
-	$(RPCGEN) -c -o $@ $<
+	$(RPCGEN) -Y /usr/bin -c -o $@ $<
 
 $(GENFILES_H): %.h: %.x $(RPCGEN)
 	test -f $@ && rm -rf $@ || true
-	$(RPCGEN) -h -o $@ $<
+	$(RPCGEN) -Y /usr/bin -h -o $@ $<
 
 #######################################################################
 # The following allows the current practice of having
