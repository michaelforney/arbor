# Copyright 2007 Bryan Østergaard
# Distributed under the terms of the GNU General Public License v2

require sourceforge easy-multibuild

SUMMARY="Perl Compatible Regular Expression library"
DESCRIPTION="
The PCRE library is a set of functions that implement regular expression
pattern matching using the same syntax and semantics as Perl 5. PCRE has its
own native API, as well as a set of wrapper functions that correspond to the
POSIX regular expression API. The PCRE library is free, even for building
commercial software.

PCRE was originally written for the Exim MTA, but is now used by many
high-profile open source projects, including Apache, PHP, KDE, Postfix, Analog,
and Nmap. PCRE has also found its way into some well known commercial products,
like Apple Safari. Some other interesting projects using PCRE include Chicken,
Ferite, Onyx, Hypermail, Leafnode, Askemos, and Wenlin.
"
HOMEPAGE="http://www.pcre.org"

REMOTE_IDS="freshmeat:pcre"
UPSTREAM_CHANGELOG="http://pcre.org/changelog.txt"
UPSTREAM_RELEASE_NOTES="http://pcre.org/news.txt"

LICENCES="BSD-3"
SLOT="0"
PLATFORMS="~amd64 ~arm ~ia64 ~ppc64 ~x86"
MYOPTIONS="multibuild_c: 32 64"

DEPENDENCIES=""

DEFAULT_SRC_CONFIGURE_PARAMS=(
    '--enable-shared'
    '--enable-utf8'
    '--enable-unicode-properties'
)

src_install() {
    easy-multibuild_src_install

    # Move libpcre to /${LIBDIR} to keep grep and thus baselayout working
    # without requiring /usr to be mounted
    edo cd "${IMAGE}"/usr/${LIBDIR}
    dodir /${LIBDIR}

    local f
    for f in libpcre*; do
        edo mv $f "${IMAGE}"/${LIBDIR}/
        dosym /${LIBDIR}/${f} /usr/${LIBDIR}/${f}
    done
}

