# Copyright 2008 Bo Ørsted Andresen <zlin@exherbo.org>
# Distributed under the terms of the GNU General Public License v2

SUMMARY="D-Bus GLib Bindings"
HOMEPAGE="http://www.freedesktop.org/wiki/Software/DBusBindings"
SOURCES="http://dbus.freedesktop.org/releases/${PN}/${P}.tar.gz"

LICENCES="|| ( GPL-2 AFL-2.1 )"
SLOT="0"
PLATFORMS="~amd64 ~x86"
MYOPTIONS="verbose"

DEPENDENCIES="
    build+run:
        >=dev-libs/glib-2.6:2
        >=sys-apps/dbus-1.1
          dev-libs/expat
    build:
          sys-devel/gettext
        >=dev-util/pkg-config-0.20
"

DEFAULT_SRC_CONFIGURE_PARAMS=(
    --enable-asserts
    --with-xml=expat
    --disable-doxygen-docs
    --disable-xml-docs
    --disable-selinux
    --disable-tests
    --with-dbus-user=messagebus
    --with-session-socket-dir=/tmp
    --with-system-socket=/var/run/dbus/system_bus_socket
)
DEFAULT_SRC_CONFIGURE_OPTION_ENABLES=( "verbose verbose-mode" )

