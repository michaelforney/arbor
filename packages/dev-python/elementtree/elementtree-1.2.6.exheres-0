require distutils

MY_P="${P}-20050316"
S="${WORKDIR}/${MY_P}"

DESCRIPTION="Light-weight python components for working with XML"
HOMEPAGE=""
SRC_URI="http://effbot.org/media/downloads/${MY_P}.tar.gz"

LICENSE="GPL-2"
SLOT="0"
PLATFORMS="~amd64 ~ia64 ~x86"
MYOPTIONS=""

DEPENDENCIES="dev-lang/python"

src_compile() {
    distutils_src_compile
}

src_install() {
    distutils_src_install
}
